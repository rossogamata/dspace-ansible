---
all:
  children:
    dspace:
      hosts:
        dspace-server:
          # For a remote server:
          # ansible_host: 192.168.1.100
          # ansible_user: ubuntu
          # ansible_ssh_private_key_file: ~/.ssh/id_rsa

          # For localhost (install on the control machine itself):
          ansible_connection: local
          ansible_user: "{{ ansible_env.USER }}"
