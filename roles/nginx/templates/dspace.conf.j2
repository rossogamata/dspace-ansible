# Managed by Ansible — do not edit manually.
# DSpace Nginx reverse proxy configuration
# Backend  (REST API): https://{{ nginx_backend_fqdn }}/server
# Frontend (Angular):  https://{{ nginx_frontend_fqdn }}/

# ── Redirect HTTP → HTTPS ─────────────────────────────────────────────────────
server {
    listen 80;
    listen [::]:80;
    server_name {{ nginx_backend_fqdn }} {{ nginx_frontend_fqdn }};
    return 301 https://$host$request_uri;
}

# ── Backend: {{ nginx_backend_fqdn }} ─────────────────────────────────────────
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name {{ nginx_backend_fqdn }};

{% if nginx_use_letsencrypt %}
    ssl_certificate     /etc/letsencrypt/live/{{ nginx_backend_fqdn }}/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/{{ nginx_backend_fqdn }}/privkey.pem;
    include             /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam         /etc/letsencrypt/ssl-dhparams.pem;
{% else %}
    ssl_certificate     {{ nginx_ssl_cert }};
    ssl_certificate_key {{ nginx_ssl_key }};
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;
{% endif %}

    # Proxy headers for Spring Boot to reconstruct the correct request URL
    proxy_set_header Host               $host;
    proxy_set_header X-Real-IP          $remote_addr;
    proxy_set_header X-Forwarded-For    $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto  https;
    proxy_set_header X-Forwarded-Host   $host;
    proxy_set_header X-Forwarded-Port   443;

    # Increase body size for large file uploads
    client_max_body_size 512m;

    # Proxy all traffic to the DSpace Spring Boot JAR on port {{ nginx_backend_port }}
    location /server {
        proxy_pass http://127.0.0.1:{{ nginx_backend_port }}/server;
        proxy_read_timeout 300;
        proxy_send_timeout 300;
        proxy_connect_timeout 60;
    }

    # Deny direct access to Solr and other internal ports
    location ~* ^/(solr|pgadmin)/ {
        return 403;
    }
}

# ── Frontend: {{ nginx_frontend_fqdn }} ───────────────────────────────────────
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name {{ nginx_frontend_fqdn }};

{% if nginx_use_letsencrypt %}
    ssl_certificate     /etc/letsencrypt/live/{{ nginx_backend_fqdn }}/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/{{ nginx_backend_fqdn }}/privkey.pem;
    include             /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam         /etc/letsencrypt/ssl-dhparams.pem;
{% else %}
    ssl_certificate     {{ nginx_ssl_cert }};
    ssl_certificate_key {{ nginx_ssl_key }};
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;
{% endif %}

    proxy_set_header Host               $host;
    proxy_set_header X-Real-IP          $remote_addr;
    proxy_set_header X-Forwarded-For    $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto  https;
    proxy_set_header X-Forwarded-Host   $host;
    proxy_set_header X-Forwarded-Port   443;

    client_max_body_size 512m;

    # Proxy to Angular SSR / PM2 on port {{ nginx_frontend_port }}
    location / {
        proxy_pass http://127.0.0.1:{{ nginx_frontend_port }}/;
        proxy_read_timeout 120;
        proxy_send_timeout 120;
        proxy_http_version 1.1;
        proxy_set_header Upgrade    $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
